name: Manual Publish
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: paused
        run: |
          echo "manual publish is paused pending the new publish pipeline."
          echo "This workflow is intentionally disabled to avoid running stale scripts."
