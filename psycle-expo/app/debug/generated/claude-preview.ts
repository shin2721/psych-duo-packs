// Generated by scripts/sync-claude-preview.mjs
export const claudePreviewCommit = "4088219";
export const claudePreviewSource = "psycle-expo/design-previews/trail-vision.html";
export const claudePreviewHtml = String.raw`<!DOCTYPE html>
<html lang="ja">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=414, initial-scale=1">
<title>Psycle Trail — Vision Concept</title>
<style>
@import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap');

:root {
  --bg: #06080f;
  --surface: #0c1018;
  --card: rgba(14, 18, 30, 0.85);
  --glass: rgba(255,255,255,0.03);
  --glass-border: rgba(255,255,255,0.06);
  --text: #f0f2f7;
  --sub: #6b7a94;
  --dim: #2e3a50;
  --accent: #c084fc;    /* soft purple */
  --accent2: #818cf8;   /* indigo */
  --warm: #fb923c;      /* warm orange */
  --mint: #34d399;
  --rose: #fb7185;
  --gold: #fbbf24;
}

* { margin: 0; padding: 0; box-sizing: border-box; }

body {
  background: #020408;
  font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
  color: var(--text);
  display: flex;
  justify-content: center;
  align-items: flex-start;
  min-height: 100vh;
  padding: 32px 16px;
  -webkit-font-smoothing: antialiased;
}

/* ═══ Device Frame ═══ */
.device {
  width: 393px;
  height: 852px;
  background: var(--bg);
  border-radius: 48px;
  overflow: hidden;
  position: relative;
  box-shadow:
    0 0 0 1px rgba(255,255,255,0.06),
    0 40px 80px -20px rgba(0,0,0,0.8),
    0 0 120px -40px rgba(192,132,252,0.08);
}
.device-scroll {
  width: 100%;
  height: 100%;
  overflow-y: auto;
  -webkit-overflow-scrolling: touch;
  scroll-behavior: smooth;
}
.device-scroll::-webkit-scrollbar { display: none; }

/* ═══ Ambient background ═══ */
.ambient {
  position: absolute;
  top: 0; left: 0; right: 0;
  height: 100%;
  pointer-events: none;
  z-index: 0;
}
.ambient .orb {
  position: absolute;
  border-radius: 50%;
  filter: blur(80px);
  opacity: 0.12;
}
.ambient .orb-1 {
  width: 300px; height: 300px;
  background: var(--accent);
  top: -80px; left: -60px;
  animation: drift 20s ease-in-out infinite;
}
.ambient .orb-2 {
  width: 200px; height: 200px;
  background: var(--accent2);
  top: 400px; right: -40px;
  animation: drift 25s ease-in-out infinite reverse;
}
.ambient .orb-3 {
  width: 250px; height: 250px;
  background: var(--rose);
  top: 900px; left: -30px;
  animation: drift 22s ease-in-out infinite 5s;
}
.ambient .orb-4 {
  width: 180px; height: 180px;
  background: var(--mint);
  top: 1400px; right: -50px;
  animation: drift 18s ease-in-out infinite 3s;
}
@keyframes drift {
  0%, 100% { transform: translate(0, 0) scale(1); }
  33% { transform: translate(20px, 30px) scale(1.1); }
  66% { transform: translate(-15px, -20px) scale(0.95); }
}

/* Stars */
.stars {
  position: absolute;
  top: 0; left: 0; right: 0; height: 100%;
  pointer-events: none;
  z-index: 0;
}
.star {
  position: absolute;
  border-radius: 50%;
  background: #fff;
  animation: twinkle var(--dur, 4s) ease-in-out infinite var(--delay, 0s);
}
@keyframes twinkle {
  0%, 100% { opacity: var(--o1, 0.15); }
  50% { opacity: var(--o2, 0.5); }
}

/* ═══ Dynamic Island ═══ */
.island {
  position: sticky;
  top: 0;
  z-index: 100;
  display: flex;
  justify-content: center;
  padding-top: 12px;
}
.island-pill {
  width: 126px;
  height: 36px;
  background: #000;
  border-radius: 20px;
}

/* ═══ Status bar ═══ */
.status-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 4px 30px 0;
  font-size: 12px;
  font-weight: 600;
  color: var(--sub);
  position: relative;
  z-index: 10;
}

/* ═══ Top nav ═══ */
.top-nav {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 16px 24px 0;
  position: relative;
  z-index: 10;
}
.top-nav .back-btn {
  width: 36px; height: 36px;
  border-radius: 12px;
  background: var(--glass);
  border: 1px solid var(--glass-border);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 16px;
  color: var(--sub);
  cursor: pointer;
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
}
.top-nav .streak-pill {
  margin-left: auto;
  display: flex;
  align-items: center;
  gap: 5px;
  padding: 5px 12px;
  border-radius: 20px;
  background: rgba(251,191,36,0.08);
  border: 1px solid rgba(251,191,36,0.15);
  font-size: 12px;
  font-weight: 700;
  color: var(--gold);
}

/* ═══ Chapter header ═══ */
.chapter-header {
  padding: 20px 24px 0;
  position: relative;
  z-index: 10;
}
.chapter-header .ch-eyebrow {
  font-size: 11px;
  font-weight: 700;
  letter-spacing: 0.16em;
  text-transform: uppercase;
  color: var(--accent);
  margin-bottom: 6px;
}
.chapter-header h1 {
  font-size: 26px;
  font-weight: 800;
  line-height: 1.15;
  letter-spacing: -0.02em;
  background: linear-gradient(135deg, var(--text), rgba(255,255,255,0.7));
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}
.chapter-header .ch-sub {
  font-size: 13px;
  color: var(--sub);
  margin-top: 6px;
  line-height: 1.4;
}

/* ═══ Progress ring ═══ */
.progress-row {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 16px 24px 4px;
  position: relative;
  z-index: 10;
}
.progress-ring {
  width: 40px; height: 40px;
  position: relative;
}
.progress-ring svg {
  transform: rotate(-90deg);
}
.progress-ring .ring-text {
  position: absolute;
  inset: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 11px;
  font-weight: 800;
  color: var(--text);
}
.progress-info {
  flex: 1;
}
.progress-info .pi-label {
  font-size: 13px;
  font-weight: 600;
  color: var(--text);
}
.progress-info .pi-sub {
  font-size: 11px;
  color: var(--sub);
  margin-top: 1px;
}

/* ═══ Trail container ═══ */
.trail {
  position: relative;
  padding: 20px 0 120px;
  z-index: 5;
}

/* Spine / connector */
.spine {
  position: absolute;
  left: 44px;
  top: 0;
  bottom: 0;
  width: 2px;
  z-index: 1;
}
.spine-segment {
  position: absolute;
  left: 0;
  width: 2px;
}
.spine-segment.done {
  background: linear-gradient(180deg, var(--accent), var(--accent2));
  opacity: 0.6;
  box-shadow: 0 0 8px rgba(192,132,252,0.2);
}
.spine-segment.future {
  background: var(--dim);
  opacity: 0.3;
}

/* ═══ Node base ═══ */
.node {
  position: relative;
  z-index: 5;
  padding: 0 20px;
}
.node-inner {
  display: flex;
  align-items: flex-start;
  gap: 16px;
  padding: 12px 0;
}

/* Circle */
.n-circle {
  width: 50px;
  height: 50px;
  border-radius: 50%;
  flex-shrink: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 20px;
  position: relative;
  z-index: 3;
  transition: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
}

/* Done */
.node.done .n-circle {
  background: linear-gradient(145deg, rgba(192,132,252,0.15), rgba(129,140,248,0.1));
  border: 1.5px solid rgba(192,132,252,0.25);
}
.node.done .n-circle .emoji { opacity: 0.7; filter: saturate(0.6); }

/* Current */
.node.current .n-circle {
  width: 56px;
  height: 56px;
  background: linear-gradient(145deg, var(--accent), var(--accent2));
  box-shadow:
    0 0 0 4px rgba(192,132,252,0.12),
    0 8px 32px -4px rgba(192,132,252,0.5),
    inset 0 1px 1px rgba(255,255,255,0.2);
  animation: breathe 3s ease-in-out infinite;
}
@keyframes breathe {
  0%, 100% {
    box-shadow:
      0 0 0 4px rgba(192,132,252,0.12),
      0 8px 32px -4px rgba(192,132,252,0.5),
      inset 0 1px 1px rgba(255,255,255,0.2);
  }
  50% {
    box-shadow:
      0 0 0 8px rgba(192,132,252,0.06),
      0 8px 40px -4px rgba(192,132,252,0.6),
      inset 0 1px 1px rgba(255,255,255,0.2);
  }
}
.node.current .n-circle .emoji { filter: brightness(1.1); }

/* Locked */
.node.locked .n-circle {
  background: rgba(255,255,255,0.02);
  border: 1.5px solid rgba(255,255,255,0.06);
}
.node.locked .n-circle .emoji { opacity: 0.18; filter: saturate(0) brightness(0.6); }

/* Milestone */
.node.milestone .n-circle {
  width: 56px;
  height: 56px;
  font-size: 22px;
}
.node.milestone.done .n-circle {
  background: linear-gradient(145deg, rgba(251,191,36,0.15), rgba(251,191,36,0.05));
  border-color: rgba(251,191,36,0.3);
}

/* ═══ Node text ═══ */
.n-body {
  flex: 1;
  padding-top: 4px;
  min-width: 0;
}
.n-title {
  font-size: 15px;
  font-weight: 700;
  line-height: 1.3;
  color: var(--text);
  letter-spacing: -0.01em;
}
.n-desc {
  font-size: 12px;
  color: var(--sub);
  line-height: 1.45;
  margin-top: 2px;
}
.n-meta {
  display: flex;
  align-items: center;
  gap: 8px;
  margin-top: 5px;
}
.n-meta .xp-badge {
  font-size: 11px;
  font-weight: 600;
  color: var(--accent);
  padding: 2px 8px;
  border-radius: 6px;
  background: rgba(192,132,252,0.08);
}
.n-meta .check-done {
  font-size: 11px;
  color: var(--mint);
  font-weight: 600;
}
.n-meta .time-badge {
  font-size: 11px;
  color: var(--sub);
}

/* Locked text */
.node.locked .n-title { color: var(--dim); }
.node.locked .n-desc { color: rgba(255,255,255,0.1); }

/* Done: subtler text */
.node.done .n-title { color: rgba(240,242,247,0.6); }
.node.done .n-desc { color: rgba(107,122,148,0.6); }

/* ═══ Current node: hero card ═══ */
.hero-card {
  position: relative;
  z-index: 5;
  margin: 4px 20px 8px;
}
.hero-card-inner {
  display: flex;
  align-items: flex-start;
  gap: 16px;
  background: var(--card);
  border: 1px solid rgba(192,132,252,0.15);
  border-radius: 20px;
  padding: 18px;
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  position: relative;
  overflow: hidden;
}
/* Subtle gradient shine */
.hero-card-inner::before {
  content: '';
  position: absolute;
  top: -40px; right: -40px;
  width: 120px; height: 120px;
  background: radial-gradient(circle, rgba(192,132,252,0.1), transparent 70%);
  pointer-events: none;
}
.hero-content {
  flex: 1;
  min-width: 0;
}
.hero-content .n-title {
  font-size: 17px;
  font-weight: 800;
  color: var(--text);
}
.hero-content .n-desc {
  font-size: 13px;
  color: var(--sub);
  margin-top: 4px;
  line-height: 1.5;
}
.hero-meta {
  display: flex;
  align-items: center;
  gap: 10px;
  margin-top: 8px;
  flex-wrap: wrap;
}
.hero-meta .pill {
  font-size: 11px;
  font-weight: 600;
  padding: 3px 10px;
  border-radius: 8px;
}
.hero-meta .pill-xp {
  color: var(--accent);
  background: rgba(192,132,252,0.1);
}
.hero-meta .pill-time {
  color: var(--sub);
  background: rgba(255,255,255,0.04);
}
.hero-meta .pill-type {
  color: var(--warm);
  background: rgba(251,146,60,0.1);
}

.hero-cta {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  margin-top: 14px;
  padding: 12px 0;
  border-radius: 14px;
  background: linear-gradient(135deg, var(--accent), var(--accent2));
  color: #fff;
  font-size: 15px;
  font-weight: 700;
  letter-spacing: -0.01em;
  cursor: pointer;
  box-shadow: 0 4px 24px -4px rgba(192,132,252,0.5);
  transition: all 0.2s;
}
.hero-cta:hover {
  transform: translateY(-1px);
  box-shadow: 0 8px 32px -4px rgba(192,132,252,0.6);
}
.hero-cta .play-icon {
  width: 20px; height: 20px;
  background: rgba(255,255,255,0.2);
  border-radius: 6px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 10px;
}

/* ═══ Section break ═══ */
.section-break {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 20px 24px 8px;
  position: relative;
  z-index: 10;
}
.section-break .sb-line {
  flex: 1;
  height: 1px;
  background: linear-gradient(90deg, transparent, var(--dim), transparent);
}
.section-break .sb-label {
  font-size: 10px;
  font-weight: 700;
  letter-spacing: 0.18em;
  text-transform: uppercase;
  color: var(--dim);
  white-space: nowrap;
}

/* ═══ Reward node special ═══ */
.node.reward .n-circle {
  background: linear-gradient(145deg, rgba(251,191,36,0.12), rgba(251,146,60,0.08));
  border: 1.5px solid rgba(251,191,36,0.2);
}
.node.reward.locked .n-circle {
  background: rgba(255,255,255,0.02);
  border: 1.5px solid rgba(251,191,36,0.08);
}

/* ═══ Game node special ═══ */
.node.game .n-circle {
  background: linear-gradient(145deg, rgba(129,140,248,0.12), rgba(99,102,241,0.08));
  border: 1.5px solid rgba(129,140,248,0.2);
}
.node.game.locked .n-circle {
  background: rgba(255,255,255,0.02);
  border: 1.5px solid rgba(129,140,248,0.08);
}

/* ═══ Bottom fade ═══ */
.bottom-fade {
  position: fixed;
  bottom: 0;
  width: 393px;
  height: 100px;
  background: linear-gradient(to top, var(--bg), transparent);
  pointer-events: none;
  z-index: 50;
  border-radius: 0 0 48px 48px;
}

/* ═══ Completion confetti hint ═══ */
.done-check {
  position: absolute;
  bottom: -2px;
  right: -2px;
  width: 18px;
  height: 18px;
  background: var(--mint);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 10px;
  color: #000;
  font-weight: 900;
  border: 2px solid var(--bg);
  z-index: 4;
}

/* ═══ Scroll hint ═══ */
.scroll-hint {
  text-align: center;
  padding: 40px 0 20px;
  position: relative;
  z-index: 10;
}
.scroll-hint span {
  font-size: 11px;
  font-weight: 600;
  color: var(--dim);
  letter-spacing: 0.1em;
}
.scroll-hint .arrow {
  display: block;
  margin-top: 4px;
  animation: bob 2s ease-in-out infinite;
  color: var(--dim);
  font-size: 14px;
}
@keyframes bob {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(4px); }
}
</style>
</head>
<body>

<div class="device">
  <div class="device-scroll">

    <!-- Ambient BG -->
    <div class="ambient">
      <div class="orb orb-1"></div>
      <div class="orb orb-2"></div>
      <div class="orb orb-3"></div>
      <div class="orb orb-4"></div>
    </div>

    <!-- Stars -->
    <div class="stars">
      <div class="star" style="left:12%;top:8%;width:1.5px;height:1.5px;--o1:0.1;--o2:0.4;--dur:5s;--delay:0s;"></div>
      <div class="star" style="left:45%;top:5%;width:1px;height:1px;--o1:0.15;--o2:0.35;--dur:4s;--delay:1s;"></div>
      <div class="star" style="left:78%;top:12%;width:2px;height:2px;--o1:0.08;--o2:0.45;--dur:6s;--delay:2s;"></div>
      <div class="star" style="left:25%;top:22%;width:1px;height:1px;--o1:0.12;--o2:0.3;--dur:5.5s;--delay:0.5s;"></div>
      <div class="star" style="left:62%;top:18%;width:1.5px;height:1.5px;--o1:0.1;--o2:0.5;--dur:4.5s;--delay:3s;"></div>
      <div class="star" style="left:88%;top:30%;width:1px;height:1px;--o1:0.15;--o2:0.35;--dur:5s;--delay:1.5s;"></div>
      <div class="star" style="left:8%;top:40%;width:1.5px;height:1.5px;--o1:0.1;--o2:0.4;--dur:4s;--delay:2.5s;"></div>
      <div class="star" style="left:52%;top:35%;width:1px;height:1px;--o1:0.08;--o2:0.3;--dur:6s;--delay:0s;"></div>
      <div class="star" style="left:35%;top:50%;width:1px;height:1px;--o1:0.12;--o2:0.35;--dur:5s;--delay:4s;"></div>
      <div class="star" style="left:72%;top:55%;width:1.5px;height:1.5px;--o1:0.1;--o2:0.45;--dur:4.5s;--delay:1s;"></div>
      <div class="star" style="left:18%;top:65%;width:1px;height:1px;--o1:0.15;--o2:0.3;--dur:5.5s;--delay:3.5s;"></div>
      <div class="star" style="left:90%;top:70%;width:1px;height:1px;--o1:0.1;--o2:0.4;--dur:4s;--delay:2s;"></div>
    </div>

    <!-- Dynamic Island -->
    <div class="island"><div class="island-pill"></div></div>

    <!-- Status bar -->
    <div class="status-row">
      <span>12:23</span>
      <span style="letter-spacing: 2px;">●●●●</span>
    </div>

    <!-- Top nav -->
    <div class="top-nav">
      <div class="back-btn">←</div>
      <div class="streak-pill">
        <span>🔥</span>
        <span>12日</span>
      </div>
    </div>

    <!-- Chapter header -->
    <div class="chapter-header">
      <div class="ch-eyebrow">Chapter 1 · メンタル</div>
      <h1>認知の再構成</h1>
      <div class="ch-sub">ネガティブ思考パターンを認識し、<br>柔軟な視点を身につける</div>
    </div>

    <!-- Progress ring -->
    <div class="progress-row">
      <div class="progress-ring">
        <svg width="40" height="40" viewBox="0 0 40 40">
          <circle cx="20" cy="20" r="16" fill="none" stroke="rgba(255,255,255,0.06)" stroke-width="3"/>
          <circle cx="20" cy="20" r="16" fill="none" stroke="url(#prog-grad)" stroke-width="3"
            stroke-dasharray="100.5" stroke-dashoffset="57.4" stroke-linecap="round"/>
          <defs>
            <linearGradient id="prog-grad" x1="0" y1="0" x2="1" y2="1">
              <stop offset="0%" stop-color="var(--accent)"/>
              <stop offset="100%" stop-color="var(--accent2)"/>
            </linearGradient>
          </defs>
        </svg>
        <div class="ring-text">3/7</div>
      </div>
      <div class="progress-info">
        <div class="pi-label">あと4レッスン</div>
        <div class="pi-sub">このチャプターを完了で ⭐ バッジ獲得</div>
      </div>
    </div>

    <!-- ═══ TRAIL ═══ -->
    <div class="trail">

      <!-- Spine segments -->
      <div class="spine">
        <div class="spine-segment done" style="top: 22px; height: 210px;"></div>
        <div class="spine-segment future" style="top: 232px; height: 900px;"></div>
      </div>

      <!-- ── Node 1: Done ── -->
      <div class="node done">
        <div class="node-inner">
          <div class="n-circle">
            <span class="emoji">💬</span>
            <div class="done-check">✓</div>
          </div>
          <div class="n-body">
            <div class="n-title">チェックイン</div>
            <div class="n-desc">今日の気分を5段階で記録</div>
            <div class="n-meta">
              <span class="xp-badge">+4 XP</span>
              <span class="check-done">完了</span>
            </div>
          </div>
        </div>
      </div>

      <!-- ── Node 2: Done ── -->
      <div class="node done">
        <div class="node-inner">
          <div class="n-circle">
            <span class="emoji">🪞</span>
            <div class="done-check">✓</div>
          </div>
          <div class="n-body">
            <div class="n-title">思考キャッチ</div>
            <div class="n-desc">自動思考を見つけてラベリング</div>
            <div class="n-meta">
              <span class="xp-badge">+12 XP</span>
              <span class="check-done">完了</span>
            </div>
          </div>
        </div>
      </div>

      <!-- ── Node 3: Done ── -->
      <div class="node done">
        <div class="node-inner">
          <div class="n-circle">
            <span class="emoji">🌬️</span>
            <div class="done-check">✓</div>
          </div>
          <div class="n-body">
            <div class="n-title">呼吸プレップ</div>
            <div class="n-desc">4-7-8 リズムで自律神経を整える</div>
            <div class="n-meta">
              <span class="xp-badge">+8 XP</span>
              <span class="check-done">完了</span>
            </div>
          </div>
        </div>
      </div>

      <!-- ── Node 4: CURRENT → hero card ── -->
      <div class="hero-card">
        <div class="hero-card-inner">
          <div class="node current" style="padding:0;">
            <div class="n-circle">
              <span class="emoji">🍃</span>
            </div>
          </div>
          <div class="hero-content">
            <div class="n-title">リフレーム・レッスン</div>
            <div class="n-desc">ネガティブな出来事を別の角度から<br>捉え直す認知的再評価の技法</div>
            <div class="hero-meta">
              <span class="pill pill-xp">+24 XP</span>
              <span class="pill pill-time">約5分</span>
              <span class="pill pill-type">MCQ</span>
            </div>
            <div class="hero-cta">
              <div class="play-icon">▶</div>
              レッスンを始める
            </div>
          </div>
        </div>
      </div>

      <!-- Section break -->
      <div class="section-break">
        <div class="sb-line"></div>
        <div class="sb-label">つづき</div>
        <div class="sb-line"></div>
      </div>

      <!-- ── Node 5: Reward (locked) ── -->
      <div class="node reward locked">
        <div class="node-inner">
          <div class="n-circle">
            <span class="emoji">🎁</span>
          </div>
          <div class="n-body">
            <div class="n-title">報酬チェスト</div>
            <div class="n-desc">ジェム & カスタムバッジ</div>
          </div>
        </div>
      </div>

      <!-- ── Node 6: Game (locked) ── -->
      <div class="node game locked">
        <div class="node-inner">
          <div class="n-circle">
            <span class="emoji">⚖️</span>
          </div>
          <div class="n-body">
            <div class="n-title">エビデンス・バランス</div>
            <div class="n-desc">根拠を天秤にかけるミニゲーム</div>
          </div>
        </div>
      </div>

      <!-- ── Node 7: Milestone (locked) ── -->
      <div class="node milestone locked">
        <div class="node-inner">
          <div class="n-circle">
            <span class="emoji">⭐</span>
          </div>
          <div class="n-body">
            <div class="n-title">チャプター復習</div>
            <div class="n-desc">まとめテスト · 全問正解でゴールドバッジ</div>
          </div>
        </div>
      </div>

      <!-- Scroll hint -->
      <div class="scroll-hint">
        <span>CHAPTER 2</span>
        <span class="arrow">↓</span>
      </div>

      <!-- ── Peek: next chapter ── -->
      <div style="padding: 0 24px; position: relative; z-index: 10;">
        <div class="ch-eyebrow" style="font-size:10px; opacity:0.4;">Chapter 2</div>
        <div style="font-size:18px; font-weight:800; color:var(--dim); margin-top:2px;">感情の理解</div>
        <div style="font-size:12px; color:rgba(255,255,255,0.12); margin-top:4px;">感情のメカニズムを学び、うまく付き合う方法を探る</div>
      </div>

      <div style="padding: 16px 20px 0;">
        <div class="node locked" style="opacity: 0.35;">
          <div class="node-inner">
            <div class="n-circle"><span class="emoji">🎭</span></div>
            <div class="n-body">
              <div class="n-title">感情ホイール</div>
              <div class="n-desc">6つの基本感情を識別する</div>
            </div>
          </div>
        </div>
        <div class="node locked" style="opacity: 0.2;">
          <div class="node-inner">
            <div class="n-circle"><span class="emoji">🌊</span></div>
            <div class="n-body">
              <div class="n-title">感情サーフィン</div>
              <div class="n-desc">波のように感情を受け流す</div>
            </div>
          </div>
        </div>
      </div>

    </div><!-- /trail -->

  </div><!-- /device-scroll -->

  <div class="bottom-fade"></div>
</div><!-- /device -->

</body>
</html>
`;
