// AUTO-GENERATED by scripts/gen-lesson-locale-index.js — do not edit manually
import study_l01_ja from "./study_l01.ja.json";

// Locale translations (fallback handled per locale export)
import study_l01_en from "./study_l01.en.json";
import study_l01_es from "./study_l01.es.json";
import study_l01_fr from "./study_l01.fr.json";

// Japanese (base) - always available
export const studyData_ja = [
  ...study_l01_ja,
];

// en - uses en where available, falls back to ja
export const studyData_en = [
  ...study_l01_en,
];

// es - uses es where available, falls back to en -> ja
export const studyData_es = [
  ...study_l01_es,
];

// fr - uses fr where available, falls back to en -> ja
export const studyData_fr = [
  ...study_l01_fr,
];

// Default export (ja for backward compatibility)
export const studyData = studyData_ja;

/**
 * Get study data for specified locale with fallback
 * Fallback order: requested -> en -> ja
 */
export function getStudyDataForLocale(locale: string): any[] {
  const lang = locale.split('-')[0].toLowerCase();

  if (lang === 'ja') {
    return studyData_ja;
  }

  if (lang === 'en') {
    return studyData_en;
  }
  if (lang === 'es') {
    return studyData_es;
  }
  if (lang === 'fr') {
    return studyData_fr;
  }

  return studyData_en;
}
