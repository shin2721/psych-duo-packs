import { hapticFeedback } from './haptics';

// Backward-compatible alias; haptics.ts is the single implementation source.
export const HapticFeedback = hapticFeedback;

export default HapticFeedback;
